# VeriGate — Database

MongoDB is the primary data store. This folder documents the planned collection
schemas (in `schemas/`) and holds optional seed data scripts (in `seeds/`) for
local development and demos.

## Status
Structure/documentation-only scaffold. No schema code has been implemented yet.
